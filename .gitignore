node_modules/
dist/
build/
.env

# Client deliverables — never publish to the live site.
# phoenixmethodseo.com is GitHub Pages: static, no auth, no way to set
# X-Robots-Tag on PDFs. Anything committed here becomes publicly fetchable.
# Keep audits/reports local or behind the Access-protected portal instead.
audits/
.vscode/
.DS_Store
_archive/
phoenix-github-upload/
.wrangler/
# wrangler local secrets
.dev.vars*
!.dev.vars.example
!.env.example
